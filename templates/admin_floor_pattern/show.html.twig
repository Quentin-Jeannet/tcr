{% extends 'base-admin.html.twig' %}

{% block title %}FloorPattern{% endblock %}

{% block body %}
    <h1>FloorPattern</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ floor_pattern.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ floor_pattern.imageName }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ floor_pattern.updatedAt ? floor_pattern.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_floor_pattern_index') }}">back to list</a>

    <a href="{{ path('app_admin_floor_pattern_edit', {'id': floor_pattern.id}) }}">edit</a>

    {{ include('admin_floor_pattern/_delete_form.html.twig') }}
{% endblock %}
